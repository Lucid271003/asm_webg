{% extends 'base.html.twig' %}
{% block stylesheets %}{% endblock %}
{% block body %}
    {{ form_start(form) }}
    <div class="form-group">
        <label for="name">Name:</label>
        {{ form_widget(form.name,{
            'attr':{
                'class':'form-control',
                'style': 'width: 10%; margin-top: 10px; margin-bottom: 5px;',
                'placeholder': 'Task name'
            }
        }) }}
        <div class="invalid-feedback">
            {{ form_errors(form.name) }}
        </div>
    </div>
    <div class="form-group">
        <label for="address">Address:</label>
        {{ form_widget(form.address,{
            'attr':{
                'class':'form-control',
                'style': 'width: 10%; margin: 5px;',
                'placeholder': 'Address'
            }
        }) }}
    </div>
    <div class="form-group">
        <label for="phoneNumber">Phone:</label>
        {{ form_widget(form.phoneNumber,{
            'attr':{
                'class':'form-control',
                'style': 'width: 10%; margin: 5px;',
                'placeholder': 'Phone'
            }
        }) }}
    </div>
    </div>
    <input type="submit" name="customer_create" value="Create" class="btn btn-primary"/>

    <ul style='list-style: none;'>
        <li>
        <a style="color:blue;"href="{{ path('customer_index') }}">Back to the list</a>
    </li>
    </ul>
    {{ form_end(form) }}
{% endblock %}


